---
플랫폼: 프로그래머스
문제 이름: 이중우선순위큐
알고리즘: 우선순위큐
tags:
  - 우선순위큐
date: 2025-09-10
aliases:
  - 우선순위큐
복습 풀이: 250910(O)
---
# 1차 풀이
```java
import java.util.*;

class Solution {
    public int[] solution(String[] operations) {
        PriorityQueue<Integer> minor = new PriorityQueue<>();
        PriorityQueue<Integer> major = new PriorityQueue<>((a, b) -> b - a);
        for (String operation : operations) {
            String[] splited = operation.split(" ");
            String key = splited[0];
            String value = splited[1];

            if (key.equals("I")) {
                minor.add(Integer.parseInt(value));
                major.add(Integer.parseInt(value));
                continue;
            }

            if (key.equals("D") && value.equals("1")) {
                Integer target = major.poll();
                minor.remove(target);
                continue;
            }

            if (key.equals("D") && value.equals("-1")) {
                Integer target = minor.poll();
                major.remove(target);
            }
        }

        int[] results = new int[2];
        results[0] = major.isEmpty() ? 0 : major.poll();
        results[1] = minor.isEmpty() ? 0 : minor.poll();

        return results;
    }
}
```
구현은 맞아도 효율성에서 틀릴거라 생각했는데 의외로 맞춘 문제.
remove에서 for문을 한번 더 돌기 때문에 O(N2)이고, 문자열 배열 operations가 최대 1,000,000임
아래는 책에서 풀이한 답 배열을 한번만 순회하므로 훨씬 효율이 좋음

```java
public class Solution {
    private static class DoublyPriorityQueue {
        private int size = 0;
        private final PriorityQueue<Integer> minPq
                = new PriorityQueue<>();
        private final PriorityQueue<Integer> maxPq
                = new PriorityQueue<>((a, b) -> b - a);

        public void add(int value) {
            minPq.add(value);
            maxPq.add(value);
            size++;
        }

        public void removeMax() {
            if (size == 0) return;
            maxPq.poll();
            if (--size == 0) {
                maxPq.clear();
                minPq.clear();
            }
        }

        public void removeMin() {
            if (size == 0) return;
            minPq.poll();
            if (--size == 0) {
                maxPq.clear();
                minPq.clear();
            }
        }

        public int max() {
            if (size == 0) return 0;
            return maxPq.peek();
        }

        public int min() {
            if (size == 0) return 0;
            return minPq.peek();
        }
    }

    public int[] solution(String[] operations) {
        DoublyPriorityQueue dpq = new DoublyPriorityQueue();
        for (String operation : operations) {
            String[] tokens = operation.split(" ");
            String command = tokens[0];
            String value = tokens[1];
            switch (command) {
                case "I" -> dpq.add(Integer.parseInt(value));
                case "D" -> {
                    if (value.equals("1")) {
                        dpq.removeMax();
                    } else {
                        dpq.removeMin();
                    }
                }
            }
        }
        return new int[]{dpq.max(), dpq.min()};
    }
}
```
