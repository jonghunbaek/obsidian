---
플랫폼: 프로그래머스
알고리즘: 해시
tags:
  - 해시
date: 2025-12-26
복습 풀이: 251226(O), 251229(O)
---
# 1차 풀이
```java
import java.util.*;

class Solution {
    public int[] solution(String[] name, int[] yearning, String[][] photo) {
        Map<String, Integer> scoreByName = new HashMap<>();
        for (int i = 0; i < name.length; i++) {
            scoreByName.put(name[i], yearning[i]);
        }

        List<Integer> results = new ArrayList<>();
        for (String[] p : photo) {
            int sum = 0;
            for (String target : p) {
                sum += scoreByName.getOrDefault(target, 0);
            }

            results.add(sum);
        }
        return results.stream()
                .mapToInt(Integer::intValue)
                .toArray();
    }
}
```

# 2차 풀이
```java
import java.util.*;
class Solution {
    public int[] solution(String[] name, int[] yearning, String[][] photo) {
        Map<String, Integer> scoreByName = new HashMap<>();
        for (int i = 0; i < name.length; i++) scoreByName.put(name[i], yearning[i]);
        int[] results = new int[photo.length];
        for (int i = 0; i < photo.length; i++) {
            int sum = 0;
            for (String target : photo[i]) sum += scoreByName.getOrDefault(target, 0);
            results[i] = sum;
        }
        
        return results;
    }
}
```