---
플랫폼: 프로그래머스
알고리즘: 문자열
tags:
  - 문자열
date: 2026-01-13
복습 풀이: 260113(O)
---
# 1차 풀이
```java

class Solution {
    public int solution(String[] babbling) {
        String[] arr = new String[] {"aya", "ye", "woo", "ma"};
        int count = 0;
        for (int i = 0; i < babbling.length; i++) {
            babbling[i] = babbling[i].replaceAll(arr[0], "0");
            babbling[i] = babbling[i].replaceAll(arr[1], "1");
            babbling[i] = babbling[i].replaceAll(arr[2], "2");
            babbling[i] = babbling[i].replaceAll(arr[3], "3");

            boolean isSafe = !babbling[i].isBlank();
            char previous = ' ';
            for (char c : babbling[i].toCharArray()) {
                if (!Character.isDigit(c) || previous == c) isSafe = false;
                previous = c;
            }

            if (isSafe) count++;
        }

        return count;
    }
}
```
상당히 억지로 푼느낌
역시 다른 풀이를 보니 정규 표현식으로 푸는 것이 정답
```java
public int solution(String[] babbling) {  
    int answer = 0;  
    for(int i=0; i<babbling.length; i++){  
        babbling[i] = babbling[i].replaceAll("ayaaya|yeye|woowoo|mama","1");  
        babbling[i] = babbling[i].replaceAll("aya|ye|woo|ma"," ");  
        babbling[i] = babbling[i].replaceAll(" ","");  
        if(babbling[i].isEmpty()) answer++;  
    }  
    return answer;  
}
```