---
플랫폼: 프로그래머스
알고리즘:
tags:
date: 2026-01-08
복습 풀이: 260108(X), 260112(O), 260117(O)
---
# 1차 풀이
```java
class Solution {
    public int solution(int number, int limit, int power) {
        int result = 0;
        for (int i = 1; i <= number; i++) {
            int count = getDivisorCount(i);
            
            if (count > limit) {
                result += power;
            } else {
                result += count;
            }
        }
        
        return result;
    }
    
    private int getDivisorCount(int target) {
        int result = 0;
        for(int i = 1; i <= Math.sqrt(target); i++) {
            if (i == Math.sqrt(target)) {
                result++;
            } else if (target % i == 0) {
                result += 2;
            }
        }
        
        return result;
    }
}
```
약수를 효율적으로 구할 방법이 떠오르지 않아 틀림

# 3차 풀이
```java
class Solution {
    public int solution(int number, int limit, int power) {
        int answer = 0;
        for (int i = 1; i <= number; i++) {
            int divisor = countDivisor(i);
            answer += divisor > limit ? power : divisor;
        }
        
        return answer;
    }
    
    private int countDivisor(int target) {
        int count = 0;
        for (int i = 1; i * i <= target; i++) {
            if (i * i == target) {
                count++;
            } else if (target % i == 0) {
                count += 2;
            }
        }
        
        return count;
    }
}
```